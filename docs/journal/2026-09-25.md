# 2026-09-25

- Terminus review of the relax-cycle stage (session `9d240f423f4a`) returned five blocking findings, all in code the stage did not touch. Vetoed by the operator on the condition that each is addressed by a later stage: per-call authentication in `NewZitiAutomation` (`zrok-one-ziti-session`), no ID-based compensation in the share handler and `DeleteWithFilter` stopping on not-found (`zrok-compensate-by-id`, `zrok-absence-is-success`), bind/unbind published inside the transaction (`zrok-notify-after-commit`), and the swallowed bind error in `processDynamicMappings` (`zrok-store-engine-parity`). Each has a stage in the route; none is to be fixed opportunistically inside an unrelated stage.
- The reviewer reports pre-existing debt in untouched code as findings against the change under review. Expect the same vetoes to recur on later stages until those stages land; no rubric change was made.
- The relax-cycle change was exercised against a real Ziti controller and a PostgreSQL store (local dev environment, `etc/dev.yml` with the `secrets` block removed): two limited accounts, one holding a public share with NULL `frontend_selection`. Over six 15-second cycles the account with the public share was retried every cycle with its journal entry retained and the public share named in the log, its private share's dial policy was recreated exactly once, the other account was relaxed and cleared on the first cycle, and the controller stayed up. The fake and the real controller agreed on the list-by-name lookup the idempotency check depends on.
- `ZROK2_API_ENDPOINT` does not override an enabled environment's stored endpoint (`env_v0_4/api.go`, `ApiEndpoint()`): the env wins. Client and admin commands for a local controller need a `HOME` whose `~/.zrok2` is not enabled elsewhere.
- Observed in passing: `zrok2 access private` that fails to bind its local port runs unaccess on the way out, which removes the frontend row but left the Ziti dial policy behind. Same family as the create-path compensation work; not fixed here.
- Before/after on the same live setup: the controller built from the committed `HEAD` (no nil guard) panicked 12 seconds after a `limit` journal row was inserted for an account holding a public share with NULL `frontend_selection`, with the stack `relaxPublicShare` → `relaxAction.HandleAccount` → `Agent.relax`. The working-tree controller on the identical rows logged the share, retained the entry, and stayed up. A limited account cannot create shares (`CanCreateShare` refuses), so a reproduction has to create the share before inserting the journal row.
