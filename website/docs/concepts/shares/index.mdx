---
sidebar_label: Shares
sidebar_position: 0
---

import DocCardList from '@theme/DocCardList';

# Shares

A share is the basic unit of zrok. When you create a share, zrok establishes a secure tunnel between your local
resource and either a public access point or another zrok user. Shares are ephemeral by default — they exist only as
long as the share command is running.

zrok supports several share types and backend modes depending on what you want to share:

<DocCardList />
