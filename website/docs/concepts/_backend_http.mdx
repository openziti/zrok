
- `proxy` mode forwards requests received by the frontend to the target server ([more](./backend-modes/http.md))
- `web` mode serves a target folder as a file index web page ([more](../how-tos/shares/share-websites-and-files.md))
- `drive` mode serves a target folder with WebDAV ([guide](../how-tos/shares/share-sync-drives.mdx))
- `caddy` mode runs the built-in Caddy server with the targeted Caddyfile ([example](pathname:///simple_reverse_proxy.Caddyfile))
